<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="head_titel">
        android:textStyle="bold"
        android:id="@+id/textView"
        android:background="@color/colorPrimary"
        android:gravity="bottom"
        android:paddingBottom="20dp"
        android:textColor="@color/head_menu"
        android:paddingLeft="@dimen/menu_margin"
        <item name="android:textAppearance">?android:attr/textAppearanceLarge</item>
        <item name="android:text">@string/hear_titel</item>
        <item name="android:textSize">@dimen/head_title</item>
    </style>

    <style name="menu_item">

    </style>

    <style name="menu_item_image">

    </style>

</resources>
